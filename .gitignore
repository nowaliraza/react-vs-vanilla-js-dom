node_modules/
dist/
*.log
.DS_Store

# agent/tool state — machine-specific, never publish
.claude/
.agents/
.codex/

# personal working notes — kept locally, not part of the published experiment.
# Delete a line to publish that file.
lessons/
reference/
